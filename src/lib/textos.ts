export const textoPuntoEncuentro = (p: string | null): string =>
  p ?? 'Punto de encuentro a confirmar al reservar';

export const textoPrecio = (p: string | null): string => p ?? 'Consultanos el valor';
